{
  "build": {
    "builder": "DOCKERFILE",
    "watchPatterns": [
      "bot.js",
      "package.json",
      "package-lock.json",
      "Dockerfile",
      "config/**",
      "data/**"
    ]
  },
  "deploy": {
    "startCommand": "npm run start",
    "restartPolicyType": "ALWAYS"
  }
}
